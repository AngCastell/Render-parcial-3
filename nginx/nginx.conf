server {
    listen 80;


    location /api/ {
        proxy_pass https://api-service-o7wg.onrender.com/api/;

        proxy_ssl_server_name on;
        proxy_set_header Host api-service-o7wg.onrender.com;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
}
